# Copy this file to .streamlit/secrets.toml locally (or paste into
# Streamlit Community Cloud's "Secrets" settings after deploying).
# Never commit the real secrets.toml — .gitignore already excludes it.
smtp_host = "smtp.gmail.com"
smtp_user = "you@yourcompany.com"
